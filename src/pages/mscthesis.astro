---
import Layout from "@/layouts/Layout.astro";
import { profile } from "@/settings";
import thesisPdf from "@/assets/TFM_Alejandro_delaCalle_TraficoMadridRedesNeuronalesGrafos_2022.pdf";

const thesisDetails = {
  title: "Traffic forecasting in the city of Madrid using Graph Neural Networks",
  author: profile.fullName,
  supervisor: "Dr. Ricardo S. Alonso Rincón",
  institution: "International University of La Rioja (UNIR)",
  year: "2022",
  abstract: `
  Traffic forecasting is a crucial component of intelligent transportation systems, enabling better traffic management and route planning. This thesis explores the application of Graph Neural Networks (GNNs) to predict traffic flow in the city of Madrid. By modeling the road network as a graph where intersections are nodes and roads are edges, we capture the spatial dependencies of traffic patterns.
  
  We implement and evaluate Spatio-Temporal Graph Convolutional Networks (ST-GCN) to handle both the spatial topology of the road network and the temporal dynamics of traffic data. The model is trained on real-world traffic data collected from sensors across Madrid.
  
  The results demonstrate that GNN-based approaches outperform traditional time-series forecasting methods by effectively leveraging the connectivity information of the road network. This work contributes to the growing field of deep learning for urban computing and provides a scalable solution for city-wide traffic prediction.
  `,
  pdfLink: thesisPdf
};
---

<Layout title={`MSc Thesis | ${profile.fullName}`}>
  <div class="max-w-4xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
    <div class="bg-base-100 shadow-xl rounded-lg overflow-hidden border border-base-200">
      <div class="p-8">
        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-6">
            <h1 class="text-3xl font-bold text-base-content mb-4 md:mb-0">{thesisDetails.title}</h1>
            <div class="w-full md:w-auto flex justify-end mt-4 md:mt-0">
              <button class="btn btn-secondary btn-sm flex items-center justify-center px-4 py-2 text-sm md:text-base rounded-md shadow-sm">
                MSc Thesis
              </button>
            </div>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8 text-sm text-base-content/70">
            <div>
                <p><span class="font-semibold">Author:</span> {thesisDetails.author}</p>
                <p><span class="font-semibold">Supervisor:</span> {thesisDetails.supervisor}</p>
            </div>
            <div class="md:text-right">
                <p><span class="font-semibold">Institution:</span> {thesisDetails.institution}</p>
                <p><span class="font-semibold">Year:</span> {thesisDetails.year}</p>
            </div>
        </div>

        <div class="divider"></div>

        <div class="prose max-w-none mb-8">
            <h3 class="text-xl font-semibold mb-4">Abstract</h3>
            <p class="text-justify whitespace-pre-line leading-relaxed">
                {thesisDetails.abstract}
            </p>
        </div>

        <div class="flex justify-center md:justify-end">
            <a href={thesisDetails.pdfLink} class="btn btn-primary gap-2" download>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12L12 16.5m0 0L7.5 12m4.5 4.5V3" />
                </svg>
                Download PDF
            </a>
        </div>
      </div>
    </div>
  </div>
</Layout>
